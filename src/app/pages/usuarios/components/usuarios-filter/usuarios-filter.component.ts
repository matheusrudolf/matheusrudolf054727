import { Component } from '@angular/core';
import { USUARIOS_FILTER_IMPORTS } from './usuarios-filter.imports';

@Component({
    selector: 'app-usuarios-filter',
    standalone: true,
    imports: [USUARIOS_FILTER_IMPORTS],
    template: ``
})
export class UsuariosFilterComponent { }
